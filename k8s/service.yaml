apiVersion: v1
kind: Service

metadata:
  name: bookingsystem-api-service

spec:
  type: LoadBalancer

  selector:
    app: bookingsystem-api

  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP